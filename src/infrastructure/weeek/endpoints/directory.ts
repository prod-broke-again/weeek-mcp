export const workspaceInfo = { path: "/ws", envelopeKey: "workspace" as const };
export const membersList = { path: "/ws/members", envelopeKey: "members" as const };
export const tagsList = { path: "/ws/tags", envelopeKey: "tags" as const };
export const currentUser = { path: "/user/me", envelopeKey: "user" as const };
